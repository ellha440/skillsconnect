<%= simple_form_for(@required_skill) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :job %>
    <%= f.input :skill %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
